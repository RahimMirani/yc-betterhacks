# Backend API URL (required for production; set at build time)
# Local dev default: http://localhost:3001
#
# Railway: In the FRONTEND service → Variables, add REACT_APP_API_URL with the
# BACKEND service's public URL (not http://0.0.0.0:8080). Get it from:
# Backend service → Settings → Networking → Domain (e.g. https://xxx.up.railway.app)
REACT_APP_API_URL=http://localhost:3001
