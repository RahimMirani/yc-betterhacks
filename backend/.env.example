# Server
# PORT: port to listen on (default 3001; Railway sets PORT automatically)
# HOST: bind address - use 127.0.0.1 or 0.0.0.0 (do not set to a port number)
PORT=3001

# Database (Neon Postgres with pgvector)
DATABASE_URL=postgresql://user:password@ep-xxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# APIs
ANTHROPIC_API_KEY=sk-ant-api03-xxx
OPENAI_API_KEY=sk-xxx

# GitHub (optional, for Gist uploads)
GITHUB_TOKEN=
GITHUB_USERNAME=

# Production / Railway
# CORS: comma-separated origins or single FRONTEND_URL (e.g. https://your-app.up.railway.app)
ALLOWED_ORIGINS=
FRONTEND_URL=

# Optional: override paths (Railway uses ephemeral disk)
# UPLOAD_DIR=/tmp/uploads
# CACHE_DIR=/tmp/cache
